apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-devspaces

resources:
  - ../../base

components:
  - ../../components/allow-autoscaling
  - ../../components/low-idle
