for i in ${PLUGINS//,/ }
do
  echo "Installing $i"
  curl -u admin:admin --data "key=$i" http://sonarqube:9000/api/plugins/install
done
curl -u admin:admin --data "" http://sonarqube:9000/api/system/restart
